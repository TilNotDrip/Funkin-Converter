package funkin.util.paths;

class Paths
{
	/**
	 * A path content handler.
	 *
	 * Returns instances of OpenFL Assets and handles caching.
	 */
	public static var content:PathContent = new PathContent();

	/**
	 * A path location handler
	 *
	 * Returns strings of locations.
	 */
	public static var location:PathLocation = new PathLocation();
}
